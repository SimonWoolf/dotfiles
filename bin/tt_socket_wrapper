#!/bin/sh

# this is run outside of a login shell, so since tt needs its own tool versions
# for dependencies, run it via mise from the project dir
mise exec --cd ~/dev/tt -- bundle exec ruby ./tt --socket 2>> ~/tt.log
